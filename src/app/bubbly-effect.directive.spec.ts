import { BubblyEffectDirective } from './bubbly-effect.directive';

describe('BubblyEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new BubblyEffectDirective();
    expect(directive).toBeTruthy();
  });
});
